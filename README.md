## Clone the repo

```bash
git clone https://github.com/hakhant21/notification.git notification

cd notification

cp .env.exmaple .env

composer install 

npm install -g pnpm 

pnpm install 

pnpm build

php artisan key:generate

php artisan migrate:fresh --seed

php artisan reverb:start
```
